<UserControl
    x:Class="WinrtCrypto.Controls.HashingUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinrtCrypto.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    
    <Grid Margin="0,0,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <TextBlock Text="Input Text" FontSize="25" Margin="10" Grid.Row="0" Grid.Column="0" x:Name="TxtInput"></TextBlock>
        <TextBox Grid.Row="0" Grid.Column="1" Width="450" Height="50" FontSize="30" AcceptsReturn="True" Margin="10"></TextBox>
        <Button Grid.Row="1" Content="Hash it and encode Base64 Format" FontSize="20" Grid.Column="0" Margin="10" x:Name="BtnHashConvert" Tapped="BtnHashConvert_Tapped" Grid.ColumnSpan="2" HorizontalAlignment="Center"></Button>
       
        
    </Grid>
</UserControl>
